#include<bits/stdc++.h>
using namespace std;

int main(){
map<int,int> mapelemet; // in the order their appera
    int n;
    cout<< "enter thevalue of n " ;
    cin >> n;
    vector<int> arr(n);
    vector<int> m(n);
    for(int i=0;i<n;i++){
        cin >> arr[i];
    }
    for(int i : arr){
        mapelemet[i]++;
    }
    for(auto freq : mapelemet){
        if(freq.second >= 1){
            cout << freq.first << " -> " << freq.second;
            cout <<" ";
        }
    }
}
